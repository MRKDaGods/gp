# Runtime requirements for local workflows and Kaggle notebooks.
# Torch/Torchvision are installed separately in Kaggle notebooks so the GPU
# build can be selected without being overwritten by pip -r.
numpy>=1.24
scipy>=1.10
pandas>=2.0
omegaconf>=2.3
jsonschema>=4.0
opencv-python-headless>=4.8
Pillow>=10.0
ultralytics>=8.1
# Cap below 13: boxmot 13+ restructured its package (boxmot.api / boxmot.detectors,
# lazy imports) and renamed tracker classes, which breaks Stage 1 tracking.
boxmot>=10.0,<13
faiss-cpu>=1.7
networkx>=3.1
matplotlib>=3.7
plotly>=5.15
seaborn>=0.12
streamlit>=1.28
sentence-transformers>=2.2
tqdm>=4.65
loguru>=0.7
click>=8.1
rich>=13.0
gdown>=4.7
kaggle>=1.6
timm>=0.9
open-clip-torch>=2.26
scikit-learn>=1.3
motmetrics>=1.4

# -- Backend web service (FastAPI app: `uvicorn backend_api:app`) --
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0

# -- Directly imported in backend/src, previously only pulled in transitively --
PyYAML>=6.0

# -- Stage-5 evaluation + extra Re-ID backbones (lazy-imported by src/) --
trackeval>=1.0           # HOTA / IDF1 / MOTA scorer (src/stage5_evaluation)
torchreid>=0.2.5         # OSNet / ResNet-IBN reid backbones (src/stage2_features, src/training)
tensorboard>=2.10        # required by torchreid's engine import
pretrainedmodels>=0.7.4  # CLIP-SENet appearance branch (src/stage2_features)