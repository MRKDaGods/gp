"""Core domain vocabulary: identifiers, typed models, time base, geometry.

Everything in ``athar.core`` is dependency-light (pydantic + numpy only) and
imported by every other subsystem. Nothing here does I/O.
"""
